# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# Agent Identity Protocol — Environment Variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── Wallet / Deployment ──────────────────────────────────
# Private key of the deployer wallet (0x-prefixed)
# Get Sepolia ETH from: https://faucet.sepolia.dev
PRIVATE_KEY=0xYOUR_DEPLOYER_PRIVATE_KEY

# ── RPC URLs ─────────────────────────────────────────────
# Sepolia — get a free key at https://alchemy.com
SEPOLIA_RPC_URL=https://eth-sepolia.g.alchemy.com/v2/YOUR_ALCHEMY_KEY

# Local Anvil (for local dev)
LOCALHOST_RPC_URL=http://127.0.0.1:8545

# ── Etherscan (contract verification) ────────────────────
# Get a free key at https://etherscan.io/myapikey
ETHERSCAN_API_KEY=YOUR_ETHERSCAN_API_KEY

# ── Database ─────────────────────────────────────────────
# PostgreSQL connection string
# Railway provides this automatically as DATABASE_URL
DATABASE_URL=postgresql://user:password@localhost:5432/agent_identity

# ── Deployed Contract Addresses ──────────────────────────
# These are filled in automatically after `forge script ... --broadcast`
# You can also find them in contracts/deployments/11155111.json (Sepolia)
DID_REGISTRY_ADDRESS=
SCHEMA_REGISTRY_ADDRESS=
ATTESTATION_REGISTRY_ADDRESS=
DELEGATION_REGISTRY_ADDRESS=
REVOCATION_REGISTRY_ADDRESS=

# ── Indexer ───────────────────────────────────────────────
CHAIN_ID=11155111
# Block number where contracts were deployed (set after deploy)
START_BLOCK=0
# How many ms between polling for new events
POLL_INTERVAL_MS=12000

# ── Resolver API ──────────────────────────────────────────
PORT=3001
HOST=0.0.0.0
CORS_ORIGIN=*

# ── IPFS (optional — for metadata pinning) ────────────────
# Get a free key at https://app.pinata.cloud
PINATA_API_KEY=
PINATA_SECRET_KEY=
